# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def shared_pods
    pod "Unbox"
    pod "PromiseKit", "~> 6.8"
    pod 'Charts'
    pod 'SwiftLint'
    pod 'lottie-ios'
    pod 'Alamofire', '~> 5.0.0-beta.2'
    pod 'Swinject'
    pod 'Differ'
end


target 'MyWork' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  shared_pods
  # Pods for MyWork
end

target 'SwiftBySundell' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    shared_pods
    # Pods for MyWork
end
